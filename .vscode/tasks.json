{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "group": "build",
            "label": "build-watch",
            "command": "cmd /C build-watch.bat",
            "problemMatcher": "$tsc-watch"
        }
    ]
}